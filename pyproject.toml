[project]
name = "monitoring-toolforge"
version = "1.0"
requires-python = ">=3.14"
dependencies = [
    "prometheus-client",
    "fastapi[standard]",
    "aiohttp",
    "asyncmy ; python_version <'4.0'",
    "PyYAML",
]
